/**
 * Placeholder for Anchor integration tests.
 * Run after `anchor build` / local validator: `anchor test` (requires Anchor CLI).
 */
describe('hivecall programs', () => {
  it('placeholder', () => {
    // TODO: deploy programs and assert initialize
  });
});
